# 2026-07-08

## 全链路巡检完善（上午 09:04-10:20）
- **并发数 4→12**：InspectionService 参数化 concurrency（`@Value("${enviro.inspection.concurrency:12}")`）
- **parseResult HEVC 过滤**：`redirectErrorStream(true)` 混入 stderr 的 HEVC 警告导致 Jackson 误判为 JSON 数组。
  修复：循环尝试每个 `{` 位置解析，直到成功。
- **超时→离线**：TimeoutException 设 `status="offline"` 而非 `"error"`。
- **台账 Word 文档生成**：`LedgerService.generateAndSave()` 原只写 DB，现用 Apache POI 基于模板 docx 真实生成文件。
  - 模板 `templates/危废仓库巡查台账_新模版.docx`（104行×7列，102路预填摄像头）
  - 根据摄像头名称匹配更新行（是否超阈值、异常描述）
  - 保存到 `./ledger/YYYY-MM-DD/台账_YYYY-MM-DD.docx`
- **LedgerController**：`GET /api/v1/ledger/{inspectId}/download` — 返回 JSON 元信息（含 fileName/docxPath）
  - `GET /api/v1/ledger/{inspectId}/file` — 直接下载 docx 二进制
- **MCP downloadLedgerDocx 全链路通**：返回 `{"ok":true, data:{inspectId, fileName, docxPath}}`

### 测试结果（id=17）
- 114 online | 1 offline（美誉化工危废仓库门口-超时）| 0 abnormal
- 台账 2 条记录（离线+低质量各1），docx 31KB
- 飞书通知发送成功
