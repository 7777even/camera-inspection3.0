# 2026-07-06

## Superpowers 技能套件安装
- 从 GitHub `obra/superpowers` (v6.1.1, MIT) 安装了 14 个开发方法论技能到 `~/.workbuddy/skills/superpowers-*`
- 安全审计：P2（安全），纯指令性文本，无恶意代码
- 技能涵盖：brainstorming、writing-plans、TDD、subagent-driven-development、systematic-debugging、code-review 等完整开发流程
- 注意：部分功能（子代理调度、Git worktree）需根据 WorkBuddy 工具能力调整

## 项目约定
- 用户要求本目录下所有开发必须遵循 Superpowers 流程（已写入 MEMORY.md）

## OpenSpec 初始化
- 运行 `openspec init --tools codebuddy`，OpenSpec v1.5.0
- 创建 `openspec/` 目录（specs/、changes/、config.yaml）
- 创建 `.codebuddy/` 下 5 个技能 + 5 个斜杠命令（opsx:propose/explore/apply/sync/archive）

## OpenSpec config.yaml 配置
- 填写完整项目上下文：三层架构（环保小脑 → 鹊桥 → 脑机端）、技术栈、数据表、开发约定
- 定制 artifact 规则：proposal（架构影响分析 + sync_version 兼容）、tasks（2h 粒度 + 分层标注）、design（数据流向 + 容错策略）
- 方案来源：`环保小脑摄像头巡检_鹊桥MCP方案_v3.0.md`

## Phase 1 OpenSpec 提案创建
- 创建变更 `phase1-enviro-brain-bootstrap`，4 个 artifact 全部完成：
  - `proposal.md`：6 个新 capability，明确同步接口（鹊桥拉取）边界
  - `design.md`：6 个技术决策（SB 3.3.x、sync_version_seq 独立序列表、分页游标、DB+Excel 双模式等）
  - `specs/`：6 个 capability 的详细规范（共 30+ 个 Requirement + Scenario）
  - `tasks.md`：9 组 37 个可追踪任务（checkbox 格式）
- 架构决策：sync_version_seq 独立表（非自增）、增量接口分页游标、DB 摄像头存储 + Excel upsert 导入
